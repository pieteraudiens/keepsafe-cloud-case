name: Showcase
on:
  push:
    branches:
      - master
env:
  SHOWCASE_ENV: lol
jobs:
  build:
    name: TODO
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
      - id: install_npm_dependencies
        run: npm install
      - id: run_tests
        run: npm run test
      - id: run_lint
        run: npm run lint
